<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="LuvaApp.TrainingPage"
             Title="TrainingPage">
    <ScrollView BackgroundColor="{StaticResource BackgroundColor}">
        <StackLayout VerticalOptions="CenterAndExpand" HorizontalOptions="CenterAndExpand" >
            <Frame BorderColor="Black" CornerRadius="2" Padding="2" >
                <Entry x:Name="LetterToTrain" 
                       Placeholder="Digite a Letra para identificar" 
                       BackgroundColor="#D1F5FD" 
                       TextColor="Black"/>
            </Frame>
            <Button
            x:Name="BluetoothBtn"
            Text="Connect to Bluetooth" 
            SemanticProperties.Hint="Connect to bluetooth device"
            Clicked="ConnectToBluetoothDevice"
            Style="{StaticResource btnStyle}" />

            <Button
            x:Name="CharacteristicBluetoothBtn"
            Text="Começar" 
            Clicked="OnComecarClicked"
            HorizontalOptions="Fill" 
            IsEnabled="False"
            Style="{StaticResource btnStyle}"/>
        </StackLayout>
    </ScrollView>

</ContentPage>